# 洗衣管家 · 照片批量上传助手 — 开发交接文档

> **交接对象**：全栈工程师 ｜ **文档日期**：2026-09-22（第二轮整理后重写） ｜ **当前版本**：v1.10
> **源码位置**：`E:\软件开发\洗衣管家上传助手`（Git 仓库）
> **远程仓库**：<https://github.com/Jace-Hao/xiyi-photo-upload>（Releases 提供全部安装包）
> **成品形态**：Windows 安装包（软件内含在线更新）；本机（门店工作机）已安装 v1.10 于 `D:\洗衣管家上传助手`

---

## 0. 一分钟读懂

- **这是什么**：Windows 桌面自动化工具。输入一份导出清单（CSV：衣物条码 + 照片文件夹路径），自动操作门店管理软件「洗衣管家」批量上传照片，并输出执行结果。v1.8 起内置在线更新。
- **技术栈**：Python 3.14 + Tkinter(GUI) + PyAutoGUI + Chrome DevTools Protocol（WebSocket）+ Win32 消息/窗口 API；分发用 PyInstaller(onedir) + Inno Setup。
- **两种运行模式**：
  1. **CDP 精准模式（首选）**：通过洗衣管家的 Chromium 调试端口（9222）做页面级控制；
  2. **图像锚点/坐标模式（兜底）**：截图模板匹配 + 坐标点击（调试端口不可用时自动切换）。
- **最重要的一条经验（务必先读 §4.3）**：洗衣管家以**管理员权限**运行，本工具也必须以**管理员权限**运行（打包参数 `--uac-admin`）。权限不同级时，Windows 会**静默拦截**所有自动化操作。
- **两条环境红线**：① `D:\洗衣管家上传助手` 是门店在用的**真实安装目录**，任何脚本/流程**不得**把它当测试残留清理；② 工作区 `.openclaw\tmp` 是与其它任务共享的**易失暂存区**，会被定期清空，重要产物不要放那里。

---

## 1. 业务背景与目标

### 1.1 背景

- 目标软件「洗衣管家」（`xygjwinapp.exe`）：洗衣门店使用的 Windows 桌面管理系统，本体基于 **CefSharp（Chromium 内核）**，程序目录 `D:\Blending_Release-6.1.17\`。
- 日常痛点：每收一件衣物需要为其上传照片。在软件「拍照页」的手工流程为：
  `输入条码 → 查询订单 → 点击【上传图片】→ 在系统文件框里进入该订单的照片文件夹 → 全选 → 确认`。
  一批订单几十件，重复劳动强度大。
- 解决方案：按《导出清单》格式批量自动执行上述流程。

### 1.2 输入 / 输出

- **输入**：导出清单 CSV（编码兼容 GBK / UTF-8，程序自动识别），列包含：
  `条码`、`照片数量`、`文件夹位置`（如 `E:\收衣照片\110000044440`）。
- **输出**：工具目录下 `输出结果\`：`执行结果_时间戳.csv/.xlsx`（逐条状态）、`运行日志_时间戳.txt`、出错截图（`出错截图\`）。

### 1.3 核心业务规则

- **防重复上传**：执行前检查订单在软件中已有的照片数；已有照片的订单默认**跳过**（`options.skip_if_has_photos=true`）。
- **部分完成处理**：上传未达预期张数时，标记“疑似部分完成 N/M 张，请人工核对”，**不盲目重传**；分批逻辑可精确定位缺失批次，便于补传。
- **按订单文件夹整批上传**：一个订单一个照片文件夹。

---

## 2. 系统架构

### 2.1 组件一览

| 文件 | 作用 | 关键点 |
|---|---|---|
| `main.py` | Tkinter 主界面：导入清单、执行控制、结果导出、**在线更新**、运行前保障 | `VERSION` 常量在此；`_auto_check_update` / `_start_update_download` |
| `automation.py` | 自动化引擎 `AutomationEngine`：批量流程、CDP 单条流程、**文件对话框操作**、图像兜底模式 | 核心复杂度集中在此 |
| `cdp_control.py` | CDP 客户端 `CDPApp`：连接调试端口、执行 JS、页面识别、点击、照片计数 | 与洗衣管家页面交互的唯一入口 |
| `updater.py` | **在线更新**：查询 GitHub Releases、多源下载（直连 → 国内加速线路，断点续传 + SHA256 校验） | `check_latest()` / `download()`；仅标准库；v1.10 升级 |
| `csv_import.py` | 清单导入：编码识别、表头识别、文件夹校验 | |
| `config_io.py` | 配置默认值与读写（`config.json`） | 新配置项先加这里的 defaults |
| `calibrate.py` | 坐标/锚点校准工具（图像兜底模式用；CDP 模式无需校准） | |
| `winmatch.py` | 窗口标题打分匹配（可靠找到洗衣管家主窗口） | `pick_best_window()` |
| `ui_theme.py` | 界面主题：色板 / 字体 / 按钮体系统一管理（v1.7 起） | 换肤只动这一个文件 |
| `assets/` | Logo 与图标（`logo.ico` 多尺寸 16–256px、`logo.png`、`logo_512.png`、顶栏徽标） | v1.9 起为「上传」语义新图标 |
| `anchors/` | 页面指纹锚点图 ×7（首页/更多页/拍照页 各含 _b 变体 + 对话框） | 图像模式页面识别 |
| `构建工具/` | 打包链四脚本 + 中文语言包 + 说明页模板（详见 §5） | 换机需调整脚本顶部路径变量 |
| `开发工具/验证脚本/` | 更新下载 / 界面文案验证脚本（详见 §5.4） | 发版后可跑一遍 |
| `CHANGELOG.md` / `程序说明.html` | 版本日志 / 面向使用者的图文说明 | `build_manual.py` 可重新生成 |

### 2.2 单条订单的完整流程（CDP 精准模式）

```
[1] 连接调试端口 9222：HTTP GET /json 找到页面 target → WebSocket 连接
[2] 导航到拍照页（路由：#/clothManage/photograph?...）
[3] DOM 级输入条码（native setter + input/change 事件）→ 点击「查询/确认」
[4] 等订单数据刷新（页面出现该条码）→ 统计已有照片数（OSS 图源去重计数）
      ├─ 已有照片（≥ 本地张数）→ 跳过（防重复）
      └─ 否则进入上传
[5] 上传阶段（每批约 5 张，见 §4.2 分批）：
      a. 自动把洗衣管家窗口置前 + 置顶（上传完成后还原）
      b. CDP 受信任点击【上传图片】→ 系统「选择图片」对话框打开
      c. 选择文件（首选"消息通道"：WM_SETTEXT 写文件名框 + BM_CLICK 点"打开"；
         备用：键盘/剪贴板；最后：导航文件夹+全选）
      d. 等对话框关闭 → 轮询页面照片数增加 → 达到预期张数才算成功
[6] 记录结果 → 下一条
```

### 2.3 可靠性设计（多层兜底）

1. **对话框选择**：消息通道（最稳）→ 键盘+剪贴板（带双重校验）→ 文件夹导航+全选（最后手段）；
2. **打开对话框**：CDP 受信任点击（重试 3 次）→ 真实鼠标点击（重试 2 次）；
3. **整轮回退**：一批选择失败会关闭对话框重开重试一次；
4. **失败不假成功**：以"页面照片数实际增加"为最终验收标准。

---

## 3. 关键配置（config.json）

| 节 | 关键项 | 说明 |
|---|---|---|
| `window` | `title_keyword` | 洗衣管家窗口关键字（默认「洗衣管家」） |
| `dialog` | `title_keywords` | 文件对话框标题关键字（默认 `选择图片/打开/Open`） |
| `options` | `skip_if_has_photos` | 已有照片跳过（默认 true，**勿轻易关闭**） |
| | `screenshot_on_error` | 出错截图（默认 true） |
| | `check_update_on_start` | 启动时自动检查更新（默认 true；可在界面底部手动【检查更新】） |
| `update` | `mirrors` | 更新下载国内加速线路列表（默认 7 条；可增删；留空 = 只用直连） |
| `cdp` | `enabled` / `port` | 精准模式开关 / 调试端口（9222） |
| | `app_exe` / `app_dir` | 洗衣管家程序路径（调试重启用） |
| | `auto_start` / `start_wait` | 未开软件时自动带调试参数启动 / 等待秒数 |
| | `force_restart` | 关不掉时自动强制结束进程（默认 true） |
| | `force_topmost` | 上传阶段自动置前置顶（默认 true） |
| `timing` | `speed_factor` 等 | 各步骤等待时间、全局速度倍率 |

---

## 4. 技术要点与深坑（重点！）

> 记录"为什么这样做"与"哪些路走不通"，改代码前务必通读。

### 4.1 CDP 精准模式（洗衣管家是 CefSharp 应用）

- **启用方式**：软件带参数启动 `--remote-debugging-port=9222`（本机桌面快捷方式已自带）。
- **连接**：`http://127.0.0.1:9222/json` 取 target → WebSocket 地址 → `Runtime.evaluate` / `Input.*` / `DOM.*` 可用。
- **页面路由**：首页 `#/`、更多页 `#/more?id=9`、拍照页 `#/clothManage/photograph?moduleid=3&templetid=3007&layoutType=1`。
- **页面关键元素**：
  - 「上传图片」按钮：`p.upload_btn`（**注意：按钮含一个图标子节点**，"叶子文本"式查找会找不到，用选择器）；
  - 照片区：`.photo_content`、缩略图列表 `.pict_list li`；
  - 条码输入框：`input[placeholder*="衣物条码号"]`，查询按钮 `p.search_btn`；
  - 照片计数：页面 `img` 的 OSS 图源去重计数（`uploaded_photo_count()`）。
- **重要教训**：
  1. **点击【上传图片】必须用"受信任点击"**：`Input.dispatchMouseEvent`（moved→pressed→released）。普通 JS `element.click()` 打不开系统文件框（浏览器要求用户手势）。
  2. **受信任点击要 fire-and-forget**：对话框一旦打开，渲染进程会被**阻塞**，后续 CDP 调用全部超时——点击事件发出后不要等响应。
  3. **对话框打开期间禁止一切 CDP 调用**（会超时并可能损伤连接）。对话框阶段全部走 Win32 API + PyAutoGUI。
  4. **"注入文件"路线已废弃**：向 `.photo_content` 的隐藏 `input[type=file]` 注入文件（`DOM.setFileInputFiles`）**不会触发组件上传**（文件塞得进、界面无反应）。不要重走。

### 4.2 系统「选择图片」对话框自动化（核心难点）

- 对话框特征：类名 `#32770`，标题「选择图片」；内容为 DirectUI 样式。
- **控件定位**（`EnumChildWindows` 枚举后代）：
  - 文件名框 = **位于对话框下半部（y > 60% 高度）的最宽 `Edit` 控件**（地址栏 Edit 在上半部分，据此排除）；
  - 「打开」按钮 = 类名 `Button`、文本含「打开」的**真实 Win32 按钮**（可发消息点击）。
- **首选通道：消息直写**（最可靠、不依赖焦点/键盘）：
  ```
  SendMessage(文件名框Edit, WM_SETTEXT, 0, "带引号的多文件全路径")
  SendMessage(打开按钮, BM_CLICK)          # 或 SendMessage(对话框, WM_COMMAND, 1, 0)
  ```
  写入后用 `SendMessageW(edit, WM_GETTEXT, ...)` **直读控件内容校验**，通过才点"打开"。
- **备用通道：键盘/剪贴板**（要求对话框在前台）：
  - 置前：`AttachThreadInput` 技巧（直接 `SetForegroundWindow` 在高负载环境常失败）；
  - 粘贴后用「**清空剪贴板** → Ctrl+A/Ctrl+C → 读回」校验（⚠️ 必须先清空，否则"复制失败"时会读到残留内容 → **假成功**）；
  - 多轮：直接粘贴 → 点击文件名框再粘贴 → Alt+N 聚焦再粘贴。
- **⚠️ 文件名框有长度上限（实测 259 字符）**：带引号的多文件全路径串过长会被截断，末尾文件变无效路径。**必须分批**：`_chunk_files_by_length()` 控制每批 **≤240 字符**（约 5 张/批），逐批重复整个对话框流程。（实测 13 张 → 分 3 批全部成功。）
- **最后手段**：导航文件夹（粘贴文件夹路径→回车）→ 点列表空白 → Ctrl+A → 回车。

### 4.3 ⭐ 权限级别（UIPI）——全项目最深的坑

- **现象**：对话框弹出后，"写内容/粘贴/回车"全部无效，界面一直停着。日志显示"校验成功"但框里仍是空的（假成功）。
- **根因**：洗衣管家程序清单强制**以管理员权限运行**；旧版工具是**普通权限**。Windows 的 **UIPI（User Interface Privilege Isolation）** 会**静默拦截**低权限进程对高权限窗口的 `SendMessage`、键盘输入、剪贴板注入——不报错、直接丢弃。
- **排查方法**：对比进程"完整性级别"（Integrity Level：`S-1-16-8192`=普通 / `S-1-16-12288`=管理员），`OpenProcessToken + TokenIntegrityLevel` 读取（参考 `main.py._proc_il_value()`）。
- **正确解法（v1.6 起）**：**工具也以管理员启动**——PyInstaller 打包加 `--uac-admin`（嵌入 requireAdministrator 清单）。
  - 本机 UAC 为"不提示直接提升"（`ConsentPromptBehaviorAdmin=0`），双击**无弹窗**、用户无感；
  - 换机器部署注意目标机 UAC 设置：标准 UAC 下会弹一次"是否允许"，属正常现象（目标软件本身也需管理员）。
- **教训**：所有"粘贴不进去/消息无反应"类问题，**先查权限级别**，再查代码。

### 4.4 调试模式自动保障（每次运行前）

- 执行前自动检查 9222 端口；不可用时按需处理：
  1. 软件未启动 → 自动 `Popen([exe, "--remote-debugging-port=9222"])` + 等端口就绪；
  2. 软件开着但无调试参数 → 确认后自动重启（正常关闭 → 6 秒未退出自动 `taskkill /F` → 带参重启，最多两次尝试）。
- 实现要点：等待期间用事件泵刷新界面（防"卡死"观感）；`tasklist/taskkill` 子进程调用加 `CREATE_NO_WINDOW`（防黑窗）。
- `启动洗衣管家-调试模式.bat` 可手动以调试模式拉起软件。

### 4.5 其它已踩的坑（速查）

- **窗口匹配**：`pygetwindow.getWindowsWithTitle('洗衣管家')` 会把工具自身窗口也匹配进来（标题包含关系）；用 `winmatch.pick_best_window()` 打分选（完全相等 > 开头 > 包含；排除工具/浏览器后缀）。
- **GitHub Release 附件名只支持 ASCII**：中文名会被服务端打碎成点号（实测）；安装包附件用英文名（`Xiyiguanjia-Uploader-Setup-vX.Y.exe`），中文显示名放附件 `label` 字段。
- **火绒安全软件**：会把含 `taskkill`+`schtasks` 特征的脚本文件当恶意行为**直接删除**；此类系统操作尽量用直接命令执行，或给目录加白名单。
- **PowerShell 中文陷阱**：中文传给内联脚本/命令行易乱码——一律用 `.py` 脚本文件 + UTF-8；写中文文件用 `utf-8-sig`。
- **页面指纹顺序**：图像兜底模式页面识别优先 `_b` 变体（规避界面遮挡）。
- **照片计数时序**：换订单后等"条码信息刷新 + 连续两次读数一致"再采纳（防读到上一单残留）。
- **升级安装**：安装器替换 exe 前先关闭运行中的工具（文件占用）。

### 4.6 在线更新与多源下载（v1.8 引入 / v1.10 升级）

- **流程**：启动静默检查（`options.check_update_on_start` 可关）+ 底部【检查更新】手动 → `check_latest()` 对比版本 → 发现新版顶部提示条 →【下载并安装】→ `updater.download()`（线程 + 队列更新界面）→ 完成后确认退出 → `subprocess.Popen` 启动安装向导。
- **多源下载策略**（源列表 = GitHub 直连 + `update.mirrors` 各国内加速线路，按顺序）：
  - 速度健康检查 `SPEED_CHECKS = ((6s, 64KB/s), (22s, 96KB/s), (55s, 64KB/s))`：低于阈值**立即换下一个源**（约 6 秒就能发现慢源）；
  - **换源断点续传**：`Range: bytes=N-` 接力下载，不重头再来；服务器不支持续传时自动降级为重新下载；
  - 全部线路都慢时，第二轮放宽限制兜底再试（`_RELAXED_CHECKS = ((90s, 12KB/s))`）；
  - **完整性校验**：Release 附带 `.sha256` 附件（或说明文本含 64 位校验值）时，下载完成自动校验，不通过则换源重试。
- **界面**：状态栏显示「GitHub 直连 / 国内加速（含线路名）+ 进度%」；日志记录每次换源。
- **实测（2026-09-22）**：真机直连变慢 → 约 6 秒自动切换 gh-proxy 加速 → 从 256KB 断点接续 → 56MB 共约 50 秒完成，SHA256 与本地一致；新增线路（ddlc / idayer / xxooo / catmak）实测 600–1000 KB/s。

### 4.7 应用图标体系（v1.9 起）

- **设计**：品牌绿圆角方砖 + 白色「上传托盘 + 向上箭头」图形；替掉旧的纯文字字标（小尺寸糊成一片）。
- **多尺寸专门优化**：16/20px 极简（粗箭头+底线）、24/32px 简化（粗箭头+浅弧托盘）、40px 以上标准版。`logo.ico` 含 9 档（16/20/24/32/40/48/64/128/256），帧格式沿用原始 PNG 压缩帧。
- **源文件**：设计工作区 `E:\软件开发\图标改版\`（含旧图标备份 `_旧图标备份/`、方案对比图、嵌入核验图）；项目成品在 `assets/`。
- **坑：Windows 图标缓存很顽固**。更新图标后如快捷方式/桌面仍显示旧图，标准修复流程：
  1. `taskkill /f /im explorer.exe`，**确认进程已退出**（循环确认，防自动重启抢占）；
  2. 删除 `%LOCALAPPDATA%\Microsoft\Windows\Explorer\iconcache*.db`（逐一确认删除成功）；
  3. 启动资源管理器 + `ie4uinit.exe -show` 刷新；必要时重建快捷方式 .lnk 文件。
  - 快速路径：重建/更新快捷方式文件本身也能解决大部分情况。

### 4.8 工作区环境注意（本机特有）

- **`D:\洗衣管家上传助手` = 门店在用的真实安装目录**（v1.10）。**任何脚本/流程不得把它当测试残留清理**（2026-09-22 曾两次误删并重装；判断前先查注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall` 的 `InstallLocation` 与快捷方式指向）。
- **`.openclaw\tmp` 是共享且易失的暂存区**：可能被并行任务/清理器清空；重要构建产物、截图、交付物一律放专用目录（`安装包/`、`交付记录/` 等），用完即复制。
- **C 盘旧版残留**：`C:\Users\haoyuwei\AppData\Local\Programs\洗衣管家上传助手`（约 197MB，v1.7 时代残留；无注册表关联、主程序已不在；内含 9/20–9/22 的历史输出与日志）。**暂未清理**——如需清理，先把其中 `输出结果\` 备份出来再删。
- **火绒**：见 §4.5；对安装包/脚本的自动处置偶有影响，如遇文件"消失"先查安全软件。

---

## 5. 构建与发布流程

> 打包脚本在 `构建工具/`（含 `README.md`、`ChineseSimplified.isl`、`manual_template.html`）。**换机器或移动项目目录后，先按 `构建工具/README.md` 修改脚本顶部的路径变量。**

### 5.1 打包链（4 步，按序执行）

| 步骤 | 脚本 | 产物 |
|---|---|---|
| 1 | `build_manual.py` | `程序说明.html`（模板 + 截图注入；先跑，供打包使用） |
| 2 | `build_app.py`（PyInstaller：`--windowed --uac-admin --icon assets\logo.ico` + hidden imports） | `pkg/dist/洗衣管家上传助手/`（onedir） |
| 3 | `assemble_payload.py` | dist 内补入 assets/anchors/config/README/说明/bat → `pkg/payload` |
| 4 | `build_installer.py`（Inno Setup + 中文语言包） | `洗衣管家上传助手_安装包_v{版本}.exe` |

> ⚠️ 中间产物位于 `.openclaw\tmp\pkg`（易被环境清理）；**构建成功后立即把安装包复制到 `安装包/` 留档**。

### 5.2 版本同步清单（每次升版必改）

1. `main.py` → `VERSION = "x.y"`；
2. `CHANGELOG.md`（新增版本节）、`README.md`（安装包名、FAQ、页脚）；
3. `构建工具/manual_template.html`（5 处版本引用：手册标题/日期行/版本徽章/安装包名/页脚）；
4. `构建工具/build_installer.py` 与 `installer.iss` → `#define MyAppVersion "x.y"`；
5. 刷新 `文档素材/主界面截图.png`（开发版实跑截图，含新版本号）。

### 5.3 发布（GitHub Release）

- 构建产物复制到 `安装包/` 留档（该目录**不入库**，与 GitHub Releases 互为备份）；
- 生成 ASCII 名副本（`Xiyiguanjia-Uploader-Setup-vX.Y.exe`）作为 Release 附件；
- **同时上传 `.sha256` 校验附件**（内容：`<sha256>  <文件名>`）——供后续版本的在线更新自动校验；
- 发布后核验：用上一版客户端跑 `check_latest('旧版本')`，应能发现新版并带 `sha256_url`。

### 5.4 验证脚本（`开发工具/验证脚本/`）

- `test_updater_download.py`：多源下载联网测试（慢源自动切换 / 断点续传 / 哈希一致），详注见脚本头；
- `test_update_ui.py`：界面文案渲染测试（模拟「切换国内加速」并在状态栏与日志显示）。
- 建议每次改更新模块或发版后各跑一遍（运行方式见该目录 `README.md`）。

### 5.5 安装策略

- v1.8 起安装向导**支持自选安装位置**：「选择目标位置」页显示，默认 `D:\洗衣管家上传助手`（不再默认 C 盘）；`UsePreviousAppDir=no`（升级时不回落旧目录）。
- 安装需权限为最低级（`PrivilegesRequired=lowest`，安装免管理员）；**程序本体启动需管理员**（§4.3）。
- 升级到不同目录时，旧目录文件不会自动删除（如需清理旧目录请手工处理）。
- 卸载：系统「设置 → 应用」或安装目录 `unins000.exe`。

---

## 6. 本机部署现状（2026-09-22 晚）

| 对象 | 位置 / 说明 |
|---|---|
| 洗衣管家 | `D:\Blending_Release-6.1.17\xygjwinapp.exe`（桌面快捷方式 `E:\桌面\xygjwinapp.exe.lnk`，已带 `--remote-debugging-port=9222`） |
| 工具源码 | `E:\软件开发\洗衣管家上传助手`（Git 仓库；remote=`https://github.com/Jace-Hao/xiyi-photo-upload.git`） |
| 已安装版本 | **v1.10** @ `D:\洗衣管家上传助手`；快捷方式：桌面（`E:\桌面`）+ 开始菜单 |
| 更新通道 | GitHub Releases（软件内【检查更新】/ 启动自动检查） |
| 旧版残留 | `C:\Users\haoyuwei\AppData\Local\Programs\洗衣管家上传助手`（v1.7 残留，未清，见 §4.8） |
| Python 环境 | Python 3.14（`py -3.14`，3.14.7）；依赖见 `requirements.txt` |
| 测试清单/照片 | 出厂清单与照片目录按需导出（照片总仓如 `E:\收衣照片`） |
| 输出结果 | 运行目录下 `输出结果\`（运行日志 / 执行结果 / 出错截图） |

---

## 7. 版本历史（修复脉络）

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 09-18 | 首版：GUI + 清单导入 + 图像锚点/坐标模式 + 批量上传 + 结果导出 |
| v1.1 | 09-20 | CDP 精准模式；调试模式自动保障；更换品牌 Logo；首个安装包 |
| v1.2 | 09-21 | 修复"未调试模式下强制重启失败/界面卡死"（正常关闭→自动强杀→带参重启；界面实时刷新） |
| v1.3 | 09-21 | **上传机制重写**：真实打开「选择图片」文件框并自动选中照片（弃用无效的注入法）；超长清单自动分批（259 字符限制） |
| v1.4 | 09-21 | 稳定性加固：**消息直写通道**（WM_SETTEXT + BM_CLICK）+ 多级重试 + 校验 |
| v1.5 | 09-21 | 上传阶段自动把洗衣管家置前 + 置顶（结束后还原） |
| v1.6 | 09-21 | **权限根因修复（UIPI）**：工具以管理员启动；修复"校验假成功"；CDP 超时自动重连 |
| v1.7 | 09-22 | 界面视觉改版：品牌绿卡片体系 + `ui_theme.py` 统一主题 |
| v1.8 | 09-22 | **在线更新**（检查/下载/安装引导，含多源下载雏形）+ **安装位置可选**（默认 D 盘） |
| v1.9 | 09-22 | **全新应用图标**（上传语义、多尺寸优化；安装器/快捷方式/窗口图标全换） |
| v1.10 | 09-22 | **更新下载升级**：秒级自动切国内加速、换源断点续传、SHA256 校验、线路扩至 7 条 |

> 各版本安装包在 GitHub Releases：<https://github.com/Jace-Hao/xiyi-photo-upload/releases>；完整细节见 `CHANGELOG.md`。

---

## 8. 已知限制与后续建议

1. **依赖洗衣管家版本**：若软件大版本升级，页面路由/元素选择器可能变化——优先更新 `cdp_control.py` 中路由与选择器；图像模式的页面指纹（`anchors/`）也需重新截取。
2. **管理员权限为硬要求**（双方都要）；换机部署注意 UAC 提示。
3. 图像兜底模式需先做坐标校准（CDP 模式无需）；分辨率/缩放变化会影响图像模式。
4. **单机单实例**假设；未实现多店并发/排队。
5. **数据质量**：条码错误时软件提示"衣物详情不存在"，工具标记失败属正常（清单数据需与系统一致）。
6. 更新下载若遇少数极端网络（所有线路都慢），会自动兜底但可能较久；实在不行到 Releases 页手动下载。
7. 建议增强（按需）：结果推送企业微信/飞书；定时任务；旧版残留（C 盘）清理流程固化。

---

## 9. 故障排查速查表

| 症状 | 检查点 |
|---|---|
| 对话框打开后填不进内容 / 停在原地 | **先查权限级别**（两者都要管理员，§4.3）；再看日志里消息通道与键盘通道是否都试过 |
| 提示"未能打开选择图片对话框" | 软件是否处于可操作状态；看重试日志；极端情况手动点一下软件窗口 |
| 连接不上 9222 | 洗衣管家是否带调试参数运行；用 `启动洗衣管家-调试模式.bat` 手动拉起；检查端口占用 |
| 全部显示"跳过：已有 N 张" | 正常防重复逻辑；确需重传见配置 `skip_if_has_photos` |
| 某单"疑似部分完成 N/M" | 看该单批次；用分批逻辑推导缺失批次，人工补传剩余张数 |
| 疑似 UIPI 拦截 | 以管理员身份重开工具（桌面快捷方式即可） |
| 日志中文乱码 | 以 UTF-8 编码打开 `运行日志_*.txt` |
| 更新下载很慢/像卡住 | 正常流程会自动切国内加速（约 6 秒判定），可在日志看换源记录；全部失败时可手动到 Releases 下载 |
| 图标显示旧样式 | 图标缓存未刷新，见 §4.7 标准流程 |

---

## 10. 快速上手（接手后的第一次运行）

1. 打开「洗衣管家」（桌面快捷方式；或开工具后由工具自动拉起）；
2. 以管理员身份打开「洗衣管家上传助手」（双击即可；本机 UAC 静默、无弹窗）；
3. 点【导入清单文件…】选择 CSV（列：条码 / 照片数量 / 文件夹位置）；
4. 先点【测试第一条】观察全流程（或勾选【演练模式】只预检不上传）；
5. 确认无误 →【▶ 开始执行】；结果到 `输出结果\` 查看。
6. 更新：软件启动会自动检查；有新版本时按顶部提示条【下载并安装】即可。

---

## 11. 交付物与文件地图

- **源码仓库**：`E:\软件开发\洗衣管家上传助手`（Git；remote = GitHub `Jace-Hao/xiyi-photo-upload`）
- **安装包**：项目 `安装包/`（v1.9、v1.10 可见；更早版本在 `安装包/历史版本/`）；GitHub Releases 为权威分发。
- **工作区相关目录**（`E:\软件开发\`）：
  - `图标改版/` —— v1.9 图标设计过程稿、旧图标备份、核验图；
  - `UI改版/` —— v1.7 界面改版预览与素材（含改版前 main.py 备份）；
  - `交付记录/` —— 交付物（如 v1.10 更新下载实测图）；
  - `.cluster/` —— 历史任务工作台（内部产物；不含当前项目关键文件）；
  - `memory/` —— 工作日志（每日记录）。
- **注意**：`.openclaw\tmp` 为共享易失目录，不放长期文件（§4.8）。

---

## 12. 整理记录（2026-09-22）

**第二轮整理（本晚，第二版交接文档同期）**——清理原则：只动可再生物与自产测试残留；删除动作全部**移入回收站**（可恢复），他人/在用文件一律不碰。

- 已清理（约 95MB，均入回收站）：
  - `.cluster\wash-label\docx_build\node_modules`（约 35MB，可再生；保留 `package.json` 即一条命令可装回）；
  - `.openclaw\tmp` 中本项目产物：v1.10 安装包副本（58MB，与 `安装包/`、GitHub 重复）、校验文件、下载测试目录与脚本、金丝雀文件（**同目录下另一任务的 `xqy-ui` 文件夹保持不动**）；
  - 项目 `__pycache__/`、0 字节控制台日志 `运行日志_启动控制台.txt`；
  - `交付记录/` 中间过程文件（截图源图、运行输出、旧测试脚本副本——脚本已归档至仓库 `开发工具/验证脚本/`）。
- 归档移动：`安装包/` 内 v1.6–v1.8 安装包移入 `安装包/历史版本/` 子目录。
- 保留未动（说明）：`洗衣管家会员导出/`（进行中）、`星期衣精致洗衣衣物照片系统/`（运行数据，约 2GB）、`水洗唛打印助手/`（另一项目）、`.openclaw-attachments/`（系统管理）、项目内 `输出结果/`（开发期测试记录）、C 盘旧版残留（含历史数据，待用户决定，§4.8）。
- 第一轮整理（同日早些时候）：开发文件夹迁移到 E 盘并全量核验（36 个受管文件 0 缺失）。

**经验固化**：`D:\洗衣管家上传助手` 清理红线（§4.8）、`.openclaw\tmp` 易失性（§4.8）已写入本文档与工作区备忘，后续任何脚本不得再犯。

---

## 13. 沟通与需求历程摘要（了解项目来龙去脉）

| 时间 | 用户需求 / 问题 | 处理结果 |
|---|---|---|
| 09-16 ~ 18 | 提供流程图（绘图1）与导出清单格式，要求开发批量上传工具 | 交付首版 v1.0 并多轮自检 |
| 09-18 | 启动报错、假成功等问题反馈 | 修复窗口识别、FailSafe、坐标校验等 |
| 09-20 | 提出用浏览器调试页实现精准控制 | CDP 精准模式落地（v1.1）+ 调试模式自动保障 |
| 09-20 | 要求更换 Logo、打包安装包 | Logo 三处应用；Inno Setup 安装包落地 |
| 09-21 | "未调试模式下强制重启无效、可能卡死" | v1.2 修复（强杀 + 重启 + 界面刷新） |
| 09-21 | 要求上传 GitHub 仓库、Releases 放安装包 + 更新日志 | 仓库建成并全量推送；v1.2 起持续发布 Release |
| 09-21 | "实际操作中没打开上传按钮，导致失败" | 诊断确认注入法无效 → v1.3 重写上传机制（真实对话框 + 分批） |
| 09-21 | "对话框能开但无法填写内容，一直停着" | v1.4 消息直写通道 + 校验；v1.5 自动置顶；**v1.6 定位并修复 UIPI 权限根因** |
| 09-21 | 全批实测 | 24 单 / 125 张照片全部上传成功 |
| 09-22 | 要求在线更新（从 GitHub 检查并安装新版）+ 安装包可选择位置、默认不装 C 盘 | v1.8（多源下载雏形 + 默认 D 盘 + 选择位置页） |
| 09-22 | 要求按「上传助手」语义设计新图标 | v1.9（多尺寸专门优化；含图标缓存刷新流程） |
| 09-22 | 要求「GitHub 不稳时自动走国内加速下载」 | v1.10（秒级切换 + 断点续传 + SHA256 校验 + 7 条线路；真机实测通过） |
| 09-22 | 要求整理文件夹、清理无用文件、出交接文档 | 本文档（v1.10 全量重写）+ 第二轮整理完成 |

---

*文档维护：随版本更新；技术细节以代码与 `CHANGELOG.md` 为准。本版由第二轮整理同步重写（v1.6 → v1.10）。*
